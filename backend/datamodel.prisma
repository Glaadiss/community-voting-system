type User {
  id: ID! @unique
  email: String! @unique
  role: String!
  name: String
  passwordHash: String!
}

type Contest {
  id: ID! @unique
  name: String!
  projects: [Project!]!
}

type Project {
  id: ID! @unique
  name: String!
  contests: [Contest!]!
}
